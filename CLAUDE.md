# Claude Code Rules

## Git
- **NEVER run `git commit` or `git push` without explicit user permission.**
- Always show what will be committed and wait for the user to say "commit" or "push" before doing so.
